登录要做一下，账号密码输入，密码要有长度限制，大概6到12位吧，还得有字母和数字混着。输错3次账号锁15分钟，锁的时候不能登录。

报表导出Excel，用户可以选日期范围，范围别超过90天，不然报错。默认展示最近30天的数据。点导出按钮就生成文件。

备份数据库，每天凌晨2点自动跑。备份文件放30天，要是备份失败得发邮件告诉管理员。

搜索框不能搜空词，关键词最长50个字。搜完按相关度排，每页20条。如果搜不到东西，显示“未找到”。

手动录入测试：有一个功能，要填名称、预算（0到10万）、开始日期（必须早于等于结束日期）。没有写优先级，随便测测。